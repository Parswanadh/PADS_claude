diff --git a/generate.py b/generate.py
index 9aaf21e..5ba5eaa 100644
--- a/generate.py
+++ b/generate.py
@@ -59,7 +59,7 @@ def load_model_and_tokenizer(args: Arguments, device: str = "auto"):
     model = transformers.AutoModelForCausalLM.from_pretrained(
         local_model_path,
         use_safetensors=True,
-        device_map="auto",
+        device_map="cpu" if device == "cpu" else "auto",
         torch_dtype=torch.float16,
     )
     model.eval()
diff --git a/self_speculation/llama_model_utils.py b/self_speculation/llama_model_utils.py
index 1ef9a53..482c972 100644
--- a/self_speculation/llama_model_utils.py
+++ b/self_speculation/llama_model_utils.py
@@ -189,16 +189,24 @@ def forward(
     )
 
     hidden_states = inputs_embeds
+    # transformers >=4.4x requires rotary position embeddings to be precomputed
+    # by the caller and passed to each decoder layer, rather than computed
+    # internally per-layer (API change since this repo was originally written).
+    position_embeddings = model.model.rotary_emb(hidden_states, position_ids)
     for decoder_layer in model.model.layers:
-        hidden_states, past_key_values = decoder_layer(
+        # transformers >=4.4x decoder layers return only (hidden_states,) and
+        # mutate the passed-in DynamicCache object in place rather than
+        # returning an updated cache (API change since this repo was written).
+        hidden_states = decoder_layer(
             hidden_states,
             attention_mask=attention_mask,
             position_ids=position_ids,
+            position_embeddings=position_embeddings,
             past_key_value=past_key_values,
             output_attentions=False,
             use_cache=True,
             padding_mask=None,
-        )
+        )[0]
 
     past_key_values = past_key_values.to_legacy_cache()
     hidden_states = model.model.norm(hidden_states)
@@ -249,16 +257,18 @@ def forward_early(
     )
 
     hidden_states = inputs_embeds
+    position_embeddings = model.model.rotary_emb(hidden_states, position_ids)
     for decoder_layer in model.model.layers[:exit_layer]:
-        hidden_states, past_key_values = decoder_layer(
+        hidden_states = decoder_layer(
             hidden_states,
             attention_mask=attention_mask,
             position_ids=position_ids,
+            position_embeddings=position_embeddings,
             past_key_value=past_key_values,
             output_attentions=False,
             use_cache=True,
             padding_mask=None,
-        )
+        )[0]
 
     past_key_values = past_key_values.to_legacy_cache()
 
@@ -338,6 +348,9 @@ def forward_remainder(
 
     next_decoder_cache = []
     hidden_states = inputs_embeds
+    early_position_ids = position_ids[:, -num_tokens_to_generate:]
+    early_position_embeddings = model.model.rotary_emb(inputs_embeds, early_position_ids)
+    full_position_embeddings = model.model.rotary_emb(inputs_embeds, position_ids)
     # TODO simplify
     full_hidden_states: Optional[torch.FloatTensor] = None
     for idx, decoder_layer in enumerate(model.model.layers):
@@ -350,16 +363,16 @@ def forward_remainder(
         if is_early_exit:
             # early hidden states: B x num_gen x C
             early_hidden_states = hidden_states[:, -num_tokens_to_generate:]
-            early_position_ids = position_ids[:, -num_tokens_to_generate:]
-            hidden_states, past_key_values = decoder_layer(
+            hidden_states = decoder_layer(
                 early_hidden_states,
                 attention_mask=early_attention_mask,
                 position_ids=early_position_ids,
+                position_embeddings=early_position_embeddings,
                 past_key_value=past_key_values,
                 output_attentions=False,
                 use_cache=True,
                 padding_mask=None,
-            )
+            )[0]
         else:
             if full_hidden_states is None and exit_query_cache is not None:
                 # first time seeing the full hidden states, we need to rely on the
@@ -372,15 +385,16 @@ def forward_remainder(
             else:
                 # we already have seen the fully hidden states we can re-use them now
                 full_hidden_states = hidden_states
-            hidden_states, past_key_values = decoder_layer(
+            hidden_states = decoder_layer(
                 full_hidden_states,
                 attention_mask=full_attention_mask,
                 position_ids=position_ids,
+                position_embeddings=full_position_embeddings,
                 past_key_value=past_key_values,
                 output_attentions=False,
                 use_cache=True,
                 padding_mask=None,
-            )
+            )[0]
 
     past_key_values = past_key_values.to_legacy_cache()
     hidden_states = model.model.norm(hidden_states)
